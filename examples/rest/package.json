{
  "name": "wezi-rest-api-example",
  "version": "1.0.0",
  "description": "wezi simple Rest API example",
  "main": "index.ts",
  "scripts": {
    "start": "cross-env NODE_ENV=production NODE_PATH=./build/src node build/bin/serve.js",
    "build": "tsc",
    "clean": "rm -rf build"
  },
  "author": "Horacio Rivero",
  "dependencies": {
    "joi": "^17.3.0",
    "wezi": "^1.0.0-alpha.12",
    "wezi-router": "^1.0.0-alpha.12"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "typescript": "^4.1.3"
  }
}
