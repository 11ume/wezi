{
  "name": "wezi-rest-example",
  "version": "1.0.0",
  "description": "wezi rest service simple example",
  "main": "index.ts",
  "scripts": {
    "start": "cross-env NODE_ENV=production NODE_PATH=./build/src node build/bin/serve.js",
    "build": "tsc",
    "clean": "rm -rf build"
  },
  "author": "Horacio Rivero",
  "dependencies": {
    "joi": "^17.3.0",
    "wezi": "^1.0.0-alpha.6",
    "wezi-receive": "^1.0.0-alpha.6",
    "wezi-router": "^1.0.0-alpha.6",
    "wezi-send": "^1.0.0-alpha.6"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "typescript": "^4.0.5"
  }
}
